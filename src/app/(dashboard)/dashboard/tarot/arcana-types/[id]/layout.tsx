import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Chỉnh sửa Arcana Type - Mystic",
  description: "Chỉnh sửa thông tin Arcana Type",
};

export default function EditArcanaTypeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 